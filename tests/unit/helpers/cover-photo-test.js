
import { coverPhoto } from 'ga-wdi-boston.file-upload-client/helpers/cover-photo';
import { module, test } from 'qunit';

module('Unit | Helper | cover photo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coverPhoto([42]);
  assert.ok(result);
});

